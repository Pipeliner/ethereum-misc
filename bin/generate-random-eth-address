#!/bin/sh
# usage:
# stdout only:
# $ generate-random-eth-address -q
# stderr as well:
# $ generate-random-eth-address

set -eu
address=0x$(hexdump -n 20 -e '5/4 "%08x" 1 "\n"' /dev/urandom)
[ $# -gt 0 ] || echo $address >&2
echo -n $address
